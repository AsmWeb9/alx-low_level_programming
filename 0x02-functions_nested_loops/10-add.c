#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @a: it is an integer input
 * @b: it is an interger input
 * Return: interger value
 */
int add(int a, int b)
{
	return (a + b);
}
